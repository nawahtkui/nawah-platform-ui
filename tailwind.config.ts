import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        nawah: {
          gold: "#C9A24D",
          silver: "#BFC3C9",
          dark: "#0A0A0A",
        },
      },
    },
  },
  plugins: [],
};

export default config;
